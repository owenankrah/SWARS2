/**
 * Loading component for the Settings page
 *
 * This component is displayed while the Settings page is loading.
 * Next.js will automatically display this component during page transitions.
 *
 * @component
 * @returns {null} Returns null as we're using a minimal loading state
 */
export default function Loading() {
  return null
}

